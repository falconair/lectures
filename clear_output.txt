# Remove results and remove metadata
find . -type f -name "*.ipynb" -exec jupyter nbconvert --to notebook \
  --ClearOutputPreprocessor.enabled=True \
  --ClearMetadataPreprocessor.enabled=True \
  --inplace {} +

# Install this Git plugin to ignore metadata only changes
pip install --upgrade nbstripout
nbstripout --install
